<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.yulicahyani.eraport">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ERaport">
        <activity android:name=".ui.raport.RaportActivity"></activity>
        <activity android:name=".ui.profil.ProfilActivity" />
        <activity android:name=".ui.login.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.inputnilai.InputNilaiActivity" />
        <activity android:name=".ui.datautama.DataUtamaActivity" />
        <activity android:name=".ui.dashboard.DashboardActivity">
        </activity>
    </application>

</manifest>